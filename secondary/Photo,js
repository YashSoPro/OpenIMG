// Photo.js
const db = require('./db');

const Photo = {
  getAll: () => {
    return db.query('SELECT * FROM photos ORDER BY votes DESC');
  },
  add: (photo) => {
    return db.query('INSERT INTO photos SET ?', photo);
  },
  // Other methods as needed
};

module.exports = Photo;
